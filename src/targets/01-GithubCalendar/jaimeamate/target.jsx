import { StyleSheet, View, Text } from 'react-native';
import GenerateData from '../data'

export default function GithubCalendar() {
    return (
        <View style={styles.container}>
            <Text style={{color: 'mediumpurple', textAlign: 'center'}}>No tengo ni pajolera idea de React pero se intenta, cuando tenga otro rato ya me pongo a sacar el componente base y despes la logica</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        width: '100%',
        borderColor: 'mediumpurple',
        borderWidth: 1,
        borderRadius: 5,
    }
});
